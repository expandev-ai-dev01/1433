import { Router } from 'express';

const router = Router();

/**
 * @remarks External routes configuration
 * Add public API routes here (e.g., health checks, public data)
 *
 * Example:
 * router.get('/public/info', publicInfoController);
 */

export default router;
