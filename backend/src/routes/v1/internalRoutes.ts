import { Router } from 'express';

const router = Router();

/**
 * @remarks Internal routes configuration
 * Add authenticated API routes here
 *
 * Example:
 * import taskRoutes from './taskRoutes';
 * router.use('/task', taskRoutes);
 */

export default router;
